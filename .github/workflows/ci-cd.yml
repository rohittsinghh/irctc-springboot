name: CI/CD Pipeline — IRCTC Spring Boot

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-push:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout source code
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Set up Java 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # 3. Build and run tests with Maven
      - name: Build with Maven
        run: mvn -B clean package

      # 4. Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: rohittsinghh
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5. Build the Docker image and push to Docker Hub
      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            rohittsinghh/irctc-springboot:latest
            rohittsinghh/irctc-springboot:${{ github.sha }}
