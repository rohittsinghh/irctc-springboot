services:
  - type: web
    name: irctc-springboot
    env: docker
    # Render checks this path to confirm the service is healthy before
    # marking a deploy as successful. /health returns {"status":"ok"}
    # immediately (no lazy-loaded beans involved).
    healthCheckPath: /health
